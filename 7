#include <iostream>
using namespace std;
int main()
{
char a;
cout << "Enter any character: ";
cin >> a;
if((a>64 && a<90) || (a>96 && a<122))
cout<<"Its a alphabet";
else
cout<<"Its not a alphabet";
return 0;
}
