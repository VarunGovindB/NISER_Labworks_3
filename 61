#include <iostream>
#include <cmath>
using namespace std;
int main() 
{
long k, j, n;
string in;
j = 0;
n = 0;
cout << "Enter a number:";
cin >> in;
for (long i = in.length() - 1; i >= 0; i--) 
{
k = pow(2, j);
if (in[i] == '1') 
{
n+= k;
}
j++;
}
cout << n;
cout << endl;
return 0;
}
