#include <iostream>
using namespace std;
int main() 
{
int a;
cout<<"Enter any integer:";
cin>>a;
if(a!=0)
{
if(a%5==0 && a%11==0)
cout<<"The integer is divisible by 5";
else
cout<<"The integer is neither divisible be 5 or 11";
}
else
cout<<"the integer is 0";
return 0;
}
