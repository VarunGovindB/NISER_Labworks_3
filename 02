#include <iostream>
using namespace std;

int main() 
{
int a,b,c;
cout<<"Enter 3 numbers:";
cin>>a>>b>>c;
if(a>b && a>c)
cout<<"The greater of the three is "<<a;
else if(b>c && b>a)
cout<<"The greater of the two is "<<b;
else
cout<<"The greater of the two is "<<c;
	return 0;
}
